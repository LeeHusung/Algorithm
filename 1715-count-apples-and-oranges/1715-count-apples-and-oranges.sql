# Write your MySQL query statement below
SELECT SUM(IFNULL(T1.APPLE_COUNT, 0) + IFNULL(T2.APPLE_COUNT, 0)) AS APPLE_COUNT, SUM(IFNULL(T1.ORANGE_COUNT, 0) + IFNULL(T2.ORANGE_COUNT, 0)) AS ORANGE_COUNT
FROM BOXES T1
LEFT JOIN CHESTS T2
ON T1.CHEST_ID = T2.CHEST_ID